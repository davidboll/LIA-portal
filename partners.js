$(() => {
  class Partner {
    constructor(id, name, email, logo, jobpost) {
      this.id = id;
      this.name = name;
      this.email = email;
      this.logo = logo;
      this.jobpost = jobpost;
    }
  }
});
